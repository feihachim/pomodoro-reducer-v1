const initialState = {
    sessionLabel: 'Session',
    sessionLength: 25,
    breakLabel: 'Break',
    breakLength: 5,
    timerLabel: 'Session',
    timerLength: 1500,
    playStatus: false,
};

export default initialState;
